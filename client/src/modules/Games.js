// Mode Components
import * as Standard from '../components/games/Standard';
import * as Slice3 from '../components/games/Slice3';

// Export all the modes. Index 0 is the default
const Games = [
	Standard,
	Slice3
];

export default Games;